{
  "name": "Page load time",
  "version": "2.0.1",
  "description": "Displays page load time in the toolbar",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "react": "^16.13.1"
  }
}
